export const environment = {
  production: true,
  // URL de l'API Spring Boot en production.
  // À adapter au déploiement (ex. https://api.mondomaine.com) si l'API
  // n'est pas exposée sur localhost:8080.
  apiUrl: 'http://localhost:8080'
};
